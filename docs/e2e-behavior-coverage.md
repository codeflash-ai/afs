# E2E Behavior Coverage

Generated from the current design and test tree.

This report tracks user-visible, connector-backed behavior. It intentionally
separates:

- **Live Notion e2e**: runs against the real Notion API and creates scratch
  Notion content.
- **Local behavior e2e**: exercises the Locality architecture with fake connectors,
  local state, or seeded filesystem mounts.
- **Manual macOS coverage**: requires a signed app, File Provider approval, and
  a user session. GitHub-hosted macOS runners compile this code but do not
  register a real File Provider domain.

## How To Run Live Notion E2E

Required environment:

```sh
export LOCALITY_NOTION_LIVE_PARENT_PAGE=...
```

`LOCALITY_NOTION_LIVE_PARENT_PAGE` must point to an active, writable Notion page
shared with the integration. The `loc-cli` live workflow and Linux FUSE live
script retrieve the parent before creating scratch content and fail immediately
if the page is archived, in trash, inaccessible, or not a Notion page.

The `loc-cli` `e2e_push_workflow` live tests resolve Notion auth the same way a
local developer install does: `NOTION_TOKEN` may be set explicitly, otherwise
the tests read the default Locality credential store at `~/.loc/credentials`
for `connection:notion-default`. Set `LOCALITY_NOTION_LIVE_CONNECTION_ID` to use
a different stored Notion connection. The connector-level
`locality-notion` live tests still require `NOTION_TOKEN`.

Optional:

```sh
export NOTION_TOKEN=...
export LOCALITY_NOTION_LIVE_DIR=/tmp/locality-notion-live
export LOCALITY_LIVE_NOTION_VFS_PUSH_PULL=1
export LOCALITY_WINDOWS_CLOUD_FILES_LIVE=1
export LOCALITY_DESKTOP_LIVE_MODE_E2E=1
export LOCALITY_DESKTOP_LIVE_MODE_E2E_PAGE=~/Library/CloudStorage/Locality/notion-main/path/to/scratch/page.md
```

Run:

```sh
cargo test -p locality-notion --test live_integrity -- --ignored --test-threads=1
cargo test -p loc-cli --test e2e_push_workflow live_ -- --ignored --test-threads=1
LOCALITY_LIVE_NOTION_VFS_PUSH_PULL=1 tests/live_notion_vfs_push_pull.sh
LOCALITY_DESKTOP_LIVE_MODE_E2E=1 \
  LOCALITY_DESKTOP_LIVE_MODE_E2E_PAGE=~/Library/CloudStorage/Locality/notion-main/path/to/scratch/page.md \
  cargo test -p locality-desktop live_mode_bidirectional_cloudstorage_markdown_e2e -- --ignored --exact --test-threads=1
pwsh ./tests/windows_cloud_files_live.ps1
```

GitHub Actions runs these from `.github/workflows/notion-live-e2e.yml` on
`main` when `NOTION_TOKEN` and `LOCALITY_NOTION_LIVE_PARENT_PAGE` are configured in
the `notion-live-e2e` environment. The mounted workflow and Linux FUSE jobs seed
`~/.loc/credentials` from `NOTION_TOKEN`, remove token environment variables,
and run against disposable scratch Notion pages through the stored-credential
path. The Windows job runs on `windows-latest` and registers a real Cloud Files
sync root against disposable scratch Notion pages. During that live Windows run,
`loc doctor --json` must pass against the same state directory after the daemon
and Cloud Files provider are running.

## Expected Behavior Coverage

Coverage labels:

- **Covered live**: exercised against real Notion and verified after API reads.
- **Partial live**: real Notion is involved, but not through the full product
  surface or not for the whole behavior.
- **Local only**: covered with fake connectors, seeded state, FUSE smoke, or
  unit/integration tests, but not against real Notion.
- **Manual only**: expected product behavior exists but is only manually tested.
- **Gap**: important behavior not yet covered by e2e tests.

| ID | Expected e2e behavior | Current live Notion coverage | Local/CI coverage | Current gap |
|---|---|---|---|---|
| E2E-001 | OAuth broker connections store refresh handles/metadata without shipping or persisting connector client secrets locally. | Local only | `crates/loc-cli/tests/connect.rs` covers broker secret separation with fake brokers; `crates/loc-cli/tests/e2e_push_workflow.rs::broker_oauth_connect_stores_refresh_handle_without_leaking_secret_material` and `crates/loc-cli/tests/e2e_push_workflow.rs::google_docs_broker_oauth_connect_stores_refresh_handle_without_leaking_secret_material` cover the Notion and Google Docs workflow paths with the file credential store and connection reports. | Add broker-backed live auth smoke tests, likely outside normal CI unless using test OAuth integrations. |
| E2E-002 | New install/onboarding can reset stale local beta state, install the terminal-visible `loc` command, then proceed through connection and mount setup. | Gap | Desktop command/unit coverage plus manual DMG testing. | Needs a desktop automation harness or scripted signed-app smoke on macOS. |
| E2E-003 | Workspace mount is created as a mount-point folder under the macOS CloudStorage Locality root, for example `Locality/notion-main`, with no Documents symlink dependency. | Gap | Mount path validation tests in `apps/desktop/src-tauri/src/main.rs`. | Real File Provider mount behavior is manual-only on macOS. |
| E2E-004 | A mounted workspace can expose top-level source directories and page/database entries without materializing every page body. | Covered live for virtual filesystem core and Linux FUSE | `architecture_behavior::local_virtual_mount_supports_browse_open_edit_review_push_round_trip`; `tests/linux_fuse_smoke.sh`; `tests/live_notion_vfs_push_pull.sh`. | Linux FUSE now has a live Notion kernel-mount path; macOS File Provider registration remains local/manual. |
| E2E-005 | Opening/listing a nested page directory lazily discovers immediate children only. | Covered live for virtual filesystem core and Linux FUSE | `architecture_behavior::local_virtual_mount_supports_browse_open_edit_review_push_round_trip`; `tests/live_notion_vfs_push_pull.sh`. | macOS File Provider shell/Finder path still needs platform e2e. |
| E2E-006 | Opening `page.md` hydrates full page content into canonical Markdown. | Covered live | Local pull and architecture behavior tests. | Covered through plain-file pull and live Linux FUSE; real File Provider open hydration remains manual/macOS. |
| E2E-007 | Pasting a Notion URL locates/prioritizes the matching local `page.md` path and reveal selects the Markdown file. | Covered live for locate and hydration priority | Search/locate helpers and desktop command code are covered locally in pieces. | Desktop Finder reveal/select assertion still needs UI automation. |
| E2E-008 | Page Markdown read supports rich Notion blocks and keeps unsupported/read-only blocks visible without lossy edits. | Covered live | Broad renderer/unit coverage. | Covered for representative block types, not every future Notion block variant. |
| E2E-009 | A read/no-op push of a rich page does not mutate Notion JSON. | Covered live | Local no-op push tests. | Covered by live cyclic diverse page test. |
| E2E-010 | File-like media references render in Markdown and downloadable media can be materialized into a local media directory for agent inspection. | Covered live | Media render/unit coverage; `crates/loc-cli/tests/e2e_push_workflow.rs::pull_materializes_and_repairs_downloaded_media_cache`; `crates/loc-cli/tests/e2e_push_workflow.rs::oversized_local_media_append_fails_without_connector_apply_and_records_failed_journal`. | Live tests cover media download and local file-like upload/reconcile; local e2e covers `.loc/media` materialization, manifest writing, missing-file repair through the mounted pull path, and the oversized local media upload guardrail before connector writes. Media pruning/eviction lifecycle is not covered. |
| E2E-011 | Editing a local page marks it pending in status before push. | Covered live | Local status and architecture behavior tests. | Live coverage uses CLI status; desktop/tray pending display remains manual. |
| E2E-012 | Diff/review planning reports intended changes before applying remote writes, and destructive plans require explicit confirmation before remote writes. | Covered live | `mount_pull_mid_page_insert_push_and_status_clean`; `crates/loc-cli/tests/e2e_push_workflow.rs::large_archive_plan_requires_confirm_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::auto_save_safe_update_reconciles_and_destructive_update_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::new_page_create_validation_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::frontmatter_remote_id_mismatch_blocks_before_journaled_apply`; push/diff tests. | Local e2e covers the daemon auto-save guardrail path plus create/frontmatter validation before journal/apply; desktop review UI is not e2e automated. |
| E2E-013 | Pushing a simple page edit writes to Notion, fetches back, and status returns clean. | Covered live | Local push workflow tests. | Covered. |
| E2E-014 | Supported rich block edits push and verify: paragraph, headings 1-4, bullet, number, todo, quote, callout, code, divider, equation, table, bookmark, embed, media URL/caption, block-type replacement, safe directive block moves, read-only `link_to_page`/database line moves, unsafe `link_to_page`/database edit/retarget blocking, unsafe `link_preview` edit/move/delete blocking, table trailing-row delete, unsafe table width/header-mode and row-identity blocking, and unsafe child-page link edit/move/delete blocking. | Covered live | Notion apply/render tests; `crates/loc-cli/tests/e2e_push_workflow.rs::live_block_type_replace_pushes_and_reconciles_notion`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_directive_block_move_pushes_and_reconciles_notion`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_page_line_move_preserves_notion_block_type`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_database_line_move_preserves_notion_block_type`; `crates/loc-cli/tests/e2e_push_workflow.rs::notion_link_to_page_label_edit_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_page_retarget_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_database_retarget_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_notion_link_labeled_like_link_to_page_can_be_edited`; `crates/loc-cli/tests/e2e_push_workflow.rs::notion_link_preview_edit_move_delete_block_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_table_trailing_row_delete_pushes_and_reconciles`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_table_width_change_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::notion_table_header_mode_change_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_table_middle_row_delete_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::notion_child_page_link_label_edit_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_child_page_link_move_blocks_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_child_page_link_delete_blocks_before_journaled_apply`. | Covered for representative edits, replacement, a childless directive move, rendered `link_to_page` and database link moves, local `link_to_page` edit and live `link_to_page`/database retarget guardrails, ordinary paragraph Notion links whose labels look like rendered `link_to_page` blocks, local `link_preview` edit/move/delete guardrails, supported table trailing-row delete, live table width and middle-row-delete guardrails, local table header-mode guardrail, and local child-page edit plus live child-page move/delete guardrails; not every color/layout option. |
| E2E-015 | Typed rich text round-trips for links, annotations, inline equations, page/database/date/user mentions. | Covered live | Notion renderer/apply tests. | Live write requires explicit IDs for page/database/user mentions. |
| E2E-016 | Database directories expose `_schema.yaml` and row Markdown files. | Covered live | Pull and schema tests. | Covered through plain-file live mounted workflow, not live File Provider. |
| E2E-017 | Database row property validation rejects invalid options before writing. | Covered live | Schema validation tests; `crates/loc-cli/tests/e2e_push_workflow.rs::live_database_row_invalid_select_option_blocks_before_journaled_apply`. | Covered through connector-level validation and a mounted live workflow that proves invalid options stop before journal/apply and leave Notion unchanged. Desktop UI path remains separate. |
| E2E-018 | Existing database rows can edit body and supported properties: title, rich text, number, select, status, multi-select, checkbox, date, URL, email, phone, external files, people IDs, relation IDs. | Covered live | Notion apply tests. | Covered for the representative property set. |
| E2E-019 | Creating a new Markdown file or row-directory `page.md` under a database directory creates a Notion row and verifies the rendered row. | Covered live | Local virtual create tests; `crates/loc-cli/tests/e2e_push_workflow.rs::multi_data_source_database_row_create_blocks_before_journaled_apply`. | Covered through live mounted workflow, with local e2e coverage proving unsupported multi-data-source row creation blocks before journal/apply. |
| E2E-020 | Local creates/renames/deletes in virtual mounts surface as pending virtual mutations. | Covered live for virtual create/rename/delete core, Linux FUSE, and Windows Cloud Files | `crates/loc-cli/tests/projection_contract.rs`; `tests/linux_fuse_smoke.sh`; `tests/live_notion_vfs_push_pull.sh`; virtual filesystem tests; `crates/loc-cli/tests/e2e_push_workflow.rs::live_page_directory_create_pushes_child_page_and_refreshes_parent`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_virtual_page_directory_rename_updates_remote_title_and_reconciles`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_virtual_page_directory_delete_archives_remote_child_page`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_virtual_database_row_directory_delete_archives_remote_row`. | macOS File Provider live create/rename/delete still needs provider-backed Notion coverage. |
| E2E-021 | Push preflight detects remote drift before writing and blocks or requires review instead of overwriting. | Covered live | `crates/loc-cli/tests/push.rs`, `crates/locality-core/tests/push_executor.rs`, Notion concurrency unit tests. | Covered with scratch page local+remote drift; desktop review UI still separate. |
| E2E-022 | Pull of a dirty local file never overwrites local pending edits and does not insert conflict markers unless remote content diverged. | Covered live | `crates/loc-cli/tests/pull.rs`; `crates/loc-cli/tests/e2e_push_workflow.rs::unresolved_pull_conflict_markers_block_push_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_dirty_pull_conflict_can_be_resolved_and_pushed`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_macos_file_provider_dirty_pull_conflict_materializes_visible_markers`; `crates/loc-cli/tests/live_workspace_mount.rs` manual/destructive. | Live scratch coverage now verifies conflict marker creation, manual resolution, push, and cleanup. Local e2e coverage verifies unresolved conflict markers block diff/push before journal or connector apply. macOS-gated live coverage verifies conflict markers are written to both the visible File Provider replica and daemon cache. |
| E2E-023 | Remote observation uses cheap metadata and does not hydrate full block bodies. | Covered live | `crates/locality-notion/tests/observe.rs::live_notion_observe_page_reads_metadata_without_hydrating_blocks`; freshness tests. | Live coverage uses a counting API wrapper against real Notion and asserts zero block-children calls. |
| E2E-024 | Freshness scheduling keeps active/pending paths hot, uses bounded work, and avoids full-workspace scans. | Partial live | `crates/localityd/tests/runtime.rs`, `scheduler.rs`, `scheduled_pull.rs`, `hydration_queue.rs`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_scheduled_pull_queues_and_applies_remote_fast_forward`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_scheduled_pull_idle_ticks_do_not_repeat_notion_enumeration_or_queue_duplicates`; `crates/loc-cli/tests/e2e_push_workflow.rs::scheduled_pull_large_workspace_stubs_metadata_and_queues_only_root_hydration`; `crates/loc-cli/tests/e2e_push_workflow.rs::scheduled_pull_idle_ticks_do_not_enumerate_or_queue_duplicate_hydrations`; `crates/loc-cli/tests/e2e_push_workflow.rs::workspace_virtual_freshness_active_and_cold_ticks_prioritize_hot_work_and_cap_scan_budget`. | Live scratch coverage verifies scheduled enumeration queues and applies remote fast-forward, and that repeated sub-interval scheduler ticks do not repeat Notion enumeration or grow duplicate queued hydration work; local e2e coverage verifies a larger scheduled tree writes metadata stubs and queues only the root policy hydration, repeated idle ticks do not enumerate or add duplicate hydration work, and workspace-virtual freshness jobs prioritize dirty/conflicted/active/remote-hint pages while capping background cold work. Long-running wall-clock/API budget assertions remain a gap. |
| E2E-025 | Clean inactive files can auto-fast-forward when remote changes, while active or locally pending files are protected. | Covered live for hydration executor behavior | `crates/localityd/tests/runtime.rs`; `crates/localityd/tests/hydration_executor.rs`; `crates/locality-core/tests/explain.rs`. | Daemon scheduler trigger remains local-only; live test covers the remote fast-forward apply/skip behavior. |
| E2E-026 | `loc inspect` explains remote-only vs local+remote divergence without mutating local or remote state. | Covered live | `crates/loc-cli/tests/inspect.rs`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_inspect_explains_remote_and_local_drift_without_mutating`; `crates/locality-core/tests/explain.rs`. | Covered through live scratch page inspection; desktop review UI remains separate. |
| E2E-027 | Push success journals the operation and enables future history/undo surfaces where possible. | Covered live | `crates/loc-cli/tests/history.rs`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_push_log_and_undo_restores_remote_content`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_directive_block_move_undo_restores_remote_order`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_undo_archive_restores_paragraph_link_without_link_to_page_promotion`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_undo_child_page_create_archives_remote_page`; `crates/loc-cli/tests/e2e_push_workflow.rs::live_undo_database_row_create_archives_remote_row`. | Covered for a reversible block update, a Notion copy+archive directive move undo, archived paragraph restore without label-based `link_to_page` promotion, archiving a created child page on undo, and archiving a created database row on undo; broader undo variants remain local-only. |
| E2E-028 | Desktop push runs asynchronously, does not freeze the UI, and briefly confirms success. | Manual only | Desktop command wiring and prior manual test. | Needs UI automation around a test backend or live Notion page. |
| E2E-029 | Tray icon appears, reflects ready/pending/error state, and idle desktop CPU remains near zero. | Manual only | Tray icon unit tests and manual CPU profile. | Needs desktop runtime smoke/perf test. |
| E2E-030 | Packaged DMG includes signed `loc`, `localityd`, File Provider extension, CLI helper, tray app, and passes notarization. | Manual/publish covered | `make publish` signing/notarization validation. | Not currently a CI e2e because it requires Apple credentials. |
| E2E-031 | `loc info` explains the local source-of-record context for a mounted page without needing a token environment variable. | Covered live | `crates/loc-cli/tests/info.rs`. | Covered through the stored-credential live `loc` binary workflow and local shape tests. |
| E2E-032 | `loc doctor` performs read-only diagnostics for state, connection, credential, and mount health without leaking credentials. | Covered live | `crates/loc-cli/tests/doctor.rs`. | Covered through the stored-credential live `loc` binary workflow and local diagnostic tests. |
| E2E-033 | `loc restore` discards local-only edits from the synced shadow, returns status clean, and does not require a remote write. | Covered live for plain-file mounted workflow and local virtual projection workflow | `crates/loc-cli/tests/restore.rs`; `crates/loc-cli/tests/e2e_push_workflow.rs::virtual_projection_restore_discards_cached_edit_and_status_returns_clean`. | Covered through the stored-credential live `loc` binary workflow, local restore tests, and an e2e virtual projection restore that rewrites the daemon content cache instead of the visible mount file. |
| E2E-034 | A read-only mounted workspace allows local inspection but blocks `diff`/`push` before journaling or remote writes. | Covered live | `crates/loc-cli/tests/push.rs::push_read_only_mount_blocks_write`; virtual filesystem read-only write/create/rename/delete tests. | Covered through a stored-credential live `loc` binary workflow that verifies Notion remains unchanged and no journal is written. |
| E2E-035 | Google Docs mounted Markdown blocks unsupported rendered inline-object/table edits/moves/deletes, invalid Locality frontmatter, and unsupported document-structure directives before journaling or connector writes. | Local only | `crates/loc-cli/tests/e2e_push_workflow.rs::google_docs_rendered_inline_object_and_table_guardrails_block_before_journaled_apply`; `crates/loc-cli/tests/e2e_push_workflow.rs::google_docs_frontmatter_and_unsupported_structure_blocks_before_journaled_apply`. | Local workflow coverage verifies `diff` and daemon-backed `push` stop before concurrency checks, journal creation, or connector apply. Live Google Docs API e2e remains a gap. |

## Live Notion Test Coverage Map

| Test | Kind | Behaviors covered |
|---|---|---|
| `crates/locality-notion/tests/live_integrity.rs::live_page_read_edit_write_verify_integrity_with_media_download` | Live connector | Fetch/render rich page content, download image/video/file/PDF/audio assets into local media files, update supported blocks, append block, fetch/verify rendered Notion content. Covers E2E-006, E2E-008, E2E-010, E2E-014. |
| `crates/locality-notion/tests/live_integrity.rs::live_database_row_property_create_edit_verify_integrity` | Live connector | Create live database, get schema, validate row frontmatter, create row, update supported properties, fetch/verify row. Covers E2E-017, E2E-018. |
| `crates/locality-notion/tests/observe.rs::live_notion_observe_page_reads_metadata_without_hydrating_blocks` | Live connector | Create scratch page with a child block, observe page metadata, update title metadata, observe changed metadata/title, and assert the live connector made zero block-children calls. Covers E2E-023. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::broker_oauth_connect_stores_refresh_handle_without_leaking_secret_material` | Local broker OAuth workflow | Runs the Notion broker OAuth connect path with a fake broker exchange and the file credential store, verifies the stored credential has an opaque refresh handle and no client secret, verifies SQLite connection/profile metadata points at the secret ref, and asserts connect/connections/connection-show/profile JSON does not expose access tokens, refresh handles, client secrets, or `secret_ref`. Covers local secret-separation behavior for E2E-001. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::google_docs_broker_oauth_connect_stores_refresh_handle_without_leaking_secret_material` | Local Google Docs broker OAuth workflow | Runs the Google Docs broker OAuth connect path with a fake broker exchange and the file credential store, verifies the stored credential has an opaque refresh handle and broker metadata, verifies SQLite connection/profile metadata points at the secret ref, and asserts connect/connections/connection-show/profile JSON does not expose access tokens, refresh handles, client secrets, or `secret_ref`. Covers local secret-separation behavior for E2E-001. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_scratch_page_mount_edit_push_verifies_notion` | Live mounted workflow, plain files | Create scratch page, mount, pull, edit Markdown, diff, dirty status, push, clean status, fetch/verify Notion. Covers E2E-006, E2E-011, E2E-012, E2E-013. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::pull_materializes_and_repairs_downloaded_media_cache` | Local mounted workflow, media cache | Serves a downloadable image from a local HTTP fixture, pulls it through the Notion connector path, verifies Markdown uses a `.loc/media` href, verifies image bytes and `manifest.json` are written, deletes the local image, and pulls again to verify the media cache is repaired. Covers the local materialization and missing-file repair side of E2E-010 without relying on external media hosts. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::oversized_local_media_append_fails_without_connector_apply_and_records_failed_journal` | Local mounted workflow, media guardrail | Appends a local `.loc/media` PDF over the 20 MB single-upload cap, verifies `diff` plans the media block create, then verifies daemon-backed `push` fails with an unsupported-media message, records a failed journal, and does not call the connector upload/append path. Covers the local oversized-upload guardrail for E2E-010 and E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::large_archive_plan_requires_confirm_before_journaled_apply` | Local mounted workflow | Pulls a page with 12 synced paragraph blocks, removes the body locally, verifies diff reports a `confirm_dangerous_plan`, verifies unconfirmed push creates no journal and makes no connector write calls, then confirms the dangerous push and verifies 12 archive calls plus clean reconciliation. Covers destructive-plan review for E2E-012 and archive guardrails for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::auto_save_safe_update_reconciles_and_destructive_update_blocks_before_journaled_apply` | Local daemon auto-save workflow | Enrolls a mounted page for auto-save, verifies a safe paragraph edit reconciles through the daemon-owned auto-save push path, then removes the synced body and verifies the destructive auto-save attempt returns `auto_save_blocked` without creating another journal entry or calling the connector. Covers Live Mode's conservative local-save side of E2E-012. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::new_page_create_validation_blocks_before_journaled_apply` | Local virtual create workflow | Records pending virtual Markdown creates, writes invalid new-page bodies/frontmatter, and verifies existing remote IDs, non-page entity types, missing titles, generated stub bodies, and pre-seeded directives all make `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, or connector apply call. Covers create validation for E2E-012 and E2E-020. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::frontmatter_remote_id_mismatch_blocks_before_journaled_apply` | Local mounted workflow | Pulls a page, changes generated `loc.id` to a different remote id, and verifies `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, or connector apply call. Covers Locality identity preservation for E2E-012. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::google_docs_rendered_inline_object_and_table_guardrails_block_before_journaled_apply` | Local Google Docs mounted workflow | Seeds Google Docs rendered inline-object and table shadows, edits, moves, and deletes those guarded rendered Markdown blocks, and verifies `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, connector concurrency check, or connector apply call. Covers E2E-035. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::google_docs_frontmatter_and_unsupported_structure_blocks_before_journaled_apply` | Local Google Docs mounted workflow | Seeds Google Docs shadows, changes invalid `loc.type`, preserves or removes generated unsupported-structure directives, and verifies `diff` and daemon-backed `push` return structured validation before a plan, journal entry, connector concurrency check, or connector apply call. Covers E2E-035. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_cli_binary_uses_stored_credential_and_pushes_scratch_page` | Live CLI binary workflow, plain files | Copies the installed `~/.loc/credentials` Notion connection secret into an isolated state root, runs the real `loc` binary with `NOTION_TOKEN` removed for `connections`, `profiles`, `connection show`, `mount`, `pull`, `info`, `doctor`, `search`, clean and dirty `inspect`, `status`, `diff`, `push`, `restore`, `log`, connector-backed `undo`, and `disconnect`, asserts command JSON does not leak credential values or `secret_ref`, fetches Notion to verify both the pushed edit and undo restoration, and verifies `disconnect` deletes the stored credential while leaving the connection metadata revoked. Covers E2E-007, E2E-011, E2E-012, E2E-013, E2E-026, E2E-027, E2E-031, E2E-032, E2E-033, and the product credential-store path. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_cli_binary_read_only_mount_blocks_push_without_remote_write` | Live CLI binary workflow, plain files | Copies the installed `~/.loc/credentials` Notion connection secret into an isolated state root, mounts a scratch Notion page as read-only with token env removed, edits the local `page.md`, asserts `diff` and `push` stop at `read_only_blocked`, verifies no journal entry is created, and fetches Notion to prove the remote page was not mutated. Covers E2E-034 and the read-only product policy. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::virtual_projection_restore_discards_cached_edit_and_status_returns_clean` | Local virtual projection workflow | Hydrates a virtual Notion page into the daemon content cache, edits that cache as a FUSE/File Provider write would, verifies status is dirty, runs `loc restore` against the visible virtual path with a state root, and verifies the cache is restored from shadow while the visible mount file is not materialized. Covers the virtual projection variant of E2E-033. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_block_type_replace_pushes_and_reconciles_notion` | Live mounted workflow, plain files | Replace a paragraph with a bullet in Markdown, verify the `replace_block` plan, push to Notion, assert create+archive apply effects, verify the remote block type and local clean reconciliation. Covers the replacement path for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_directive_block_move_pushes_and_reconciles_notion` | Live mounted workflow, plain files | Move an unchanged `table_of_contents` directive line, verify the `move_block` plan, push through Notion's copy+archive apply path, and assert the reconciled local file is clean with the refreshed directive before the paragraph. Covers the directive move path for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_page_line_move_preserves_notion_block_type` | Live mounted workflow, plain files | Move a rendered `link_to_page` Markdown line, verify the planner emits append+archive, push through the preservation path, and assert Notion still has a `link_to_page` block targeting the original page. Covers the read-only link move path for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_database_line_move_preserves_notion_block_type` | Live mounted workflow, plain files | Move a rendered database link Markdown line, verify the planner emits append+archive, push through the preservation path, and assert Notion still has a `link_to_page` block targeting the original database. Covers the database-link move path for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::notion_link_to_page_label_edit_blocks_before_journaled_apply` | Local mounted workflow | Seeds a rendered Notion `link_to_page` shadow, edits only the Markdown label, and verifies `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, or connector apply call. Covers the unsafe `link_to_page` edit guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_page_retarget_blocks_before_journaled_apply` | Live mounted workflow, plain files | Retarget a rendered `link_to_page` Markdown line, assert diff/push report structured validation instead of creating a journal, and verify the live Notion source blocks are unchanged. Covers the unsafe `link_to_page` retarget guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_link_to_database_retarget_blocks_before_journaled_apply` | Live mounted workflow, plain files | Retarget a rendered database link Markdown line, assert diff/push report structured validation instead of creating a journal, and verify the live Notion source blocks are unchanged. Covers the unsafe database-link retarget guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_notion_link_labeled_like_link_to_page_can_be_edited` | Live mounted workflow, plain files | Create a real paragraph containing a Notion URL link labeled `Linked page`, edit the link target through Markdown, and verify Notion keeps it as a paragraph block rather than tripping the rendered `link_to_page` guardrail. Covers the paragraph/link false-positive regression for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::notion_link_preview_edit_move_delete_block_before_journaled_apply` | Local mounted workflow | Seeds rendered Notion `link_preview` shadows, edits, moves, and deletes the preview line in mounted Markdown, and verifies `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, or connector apply call. Covers the unsafe `link_preview` edit/move/delete guardrails for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_link_with_parentheses_href_can_be_edited` | Live mounted workflow, plain files | Edit the label of a paragraph link whose URL contains a literal closing parenthesis, then verify Notion still stores the original URL and the reconciled Markdown keeps the escaped href. Covers Markdown link parsing edge cases for E2E-014 and E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_literal_break_tag_edits_preserve_literal_text` | Live mounted workflow, plain files | Edit a paragraph containing a literal `<br>` marker and verify it remains plain paragraph text after push/reconcile. Covers escaped literal HTML marker preservation for E2E-014 and E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_literal_underline_tag_edits_preserve_literal_text` | Live mounted workflow, plain files | Edit a paragraph containing literal `<u>...</u>` text and verify it does not become Notion underline formatting. Covers escaped literal HTML marker preservation for E2E-014 and E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_literal_equation_marker_edits_preserve_literal_text` | Live mounted workflow, plain files | Edit a paragraph containing literal `$...$` text and verify it stays plain text instead of becoming equation rich text. Covers escaped literal inline equation marker preservation for E2E-014 and E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_literal_explicit_mention_marker_edits_preserve_literal_text` | Live mounted workflow, plain files | Edit a paragraph containing literal `@date(...)` text and verify it stays plain text instead of becoming a Notion mention. Covers escaped explicit mention marker preservation for E2E-014 and E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_literal_markdown_inline_marker_edits_preserve_literal_text` | Live mounted workflow, plain files | Edit a paragraph containing literal Markdown inline markers for bold, italic, strike, code, and link syntax, then verify they remain unformatted plain text in Notion. Covers escaped literal Markdown marker preservation for E2E-014 and E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_paragraph_literal_block_marker_edits_preserve_paragraph_text` | Live mounted workflow, plain files | Edit a paragraph beginning with an escaped heading marker and verify the push updates a paragraph instead of replacing it with a heading block. Covers escaped literal block marker preservation for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_table_trailing_row_delete_pushes_and_reconciles` | Live mounted workflow, plain files | Delete the trailing row from a rendered Notion table, verify the table update plan, push to Notion, and assert the row is gone from the reconciled remote Markdown while the kept row remains. Covers the supported table row delete path for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_table_width_change_blocks_before_journaled_apply` | Live mounted workflow, plain files | Change a rendered table's column count, assert diff/push report structured validation instead of creating a journal, and verify the live Notion source blocks are unchanged. Covers the unsafe table shape guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::notion_table_header_mode_change_blocks_before_journaled_apply` | Local mounted workflow | Seeds a Notion table shadow without a column header, edits the mounted Markdown to turn header cells on, and verifies `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, or connector apply call. Covers the unsafe table header-mode guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_table_middle_row_delete_blocks_before_journaled_apply` | Live mounted workflow, plain files | Delete an unchanged middle row from a rendered table, assert diff/push report structured validation instead of shifting row identities and creating a journal, and verify the live Notion source blocks are unchanged. Covers the unsafe table row-identity guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::notion_child_page_link_label_edit_blocks_before_journaled_apply` | Local mounted workflow | Seeds a rendered Notion child-page link, edits only the Markdown label, and verifies `diff` and daemon-backed `push` return structured validation without a plan, journal entry, push id, or connector apply call. Covers the unsafe child-page link edit guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_child_page_link_move_blocks_before_journaled_apply` | Live mounted workflow, plain files | Move a rendered child-page Markdown link, assert diff/push report structured validation instead of creating a journal, and verify the live Notion parent blocks are unchanged. Covers the unsafe child-page link move guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_child_page_link_delete_blocks_before_journaled_apply` | Live mounted workflow, plain files | Delete a rendered child-page Markdown link, assert diff/push report structured validation instead of creating a journal, verify the live Notion parent blocks are unchanged, and verify the child page remains unarchived. Covers the unsafe child-page link delete guardrail for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_lazy_virtual_mount_enumerates_children_and_hydrates_on_open` | Live virtual filesystem core | Create scratch page tree, enumerate virtual source root and page directory without content materialization, then hydrate on open and verify Markdown. Covers E2E-004, E2E-005, E2E-006. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_drift_preflight_blocks_push_before_overwriting_remote` | Live mounted workflow, plain files | Pull scratch page, edit locally, mutate the same page remotely, assert push blocks before overwrite, fetch/verify remote edit remains. Covers E2E-021. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_dirty_pull_conflict_can_be_resolved_and_pushed` | Live mounted workflow, plain files | Pull scratch page, edit locally, mutate the same block remotely, pull conflict markers, resolve the Markdown, push, fetch/verify resolved Notion content. Covers E2E-022 and conflict recovery. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::unresolved_pull_conflict_markers_block_push_before_journaled_apply` | Local mounted workflow | Pulls a same-block local/remote conflict into Markdown conflict markers, verifies `diff` and `push` return `fix_validation`, asserts no journal entry is created, and proves the fake Notion connector receives no write calls. Covers unresolved conflict-marker push blocking for E2E-022. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_macos_file_provider_dirty_pull_conflict_materializes_visible_markers` | Live macOS File Provider projection workflow | Pulls a live Notion page into the daemon content cache, simulates a visible File Provider replica with a missed local edit, mutates the same block remotely, then verifies pull writes conflict markers to both the visible file and daemon cache. Covers the macOS visible-replica conflict materialization side of E2E-022. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_virtual_page_directory_delete_archives_remote_child_page` | Live virtual filesystem core | Hydrate a child page, record a virtual page-directory delete, verify the pending archive status and `archive_entity` plan, push to Notion, and verify the child page is archived and the local mutation is reconciled. Covers the virtual delete/archive path for E2E-020. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_virtual_database_row_directory_delete_archives_remote_row` | Live virtual filesystem core | Hydrate a database row, record a virtual row-directory delete from the database directory, verify the pending archive status and `archive_entity` plan, push to Notion, and verify the row page is archived and the local mutation is reconciled. Covers the database-row variant of E2E-020. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_virtual_page_directory_rename_updates_remote_title_and_reconciles` | Live virtual filesystem core | Hydrate a child page, rename its virtual page directory, verify a pending `Rename` mutation, push the title change to Notion, and verify the remote title plus local clean reconciliation. Covers the virtual rename path for E2E-020. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_database_row_invalid_select_option_blocks_before_journaled_apply` | Live mounted workflow, plain files | Hydrates an existing database row, edits a select property to an option absent from `_schema.yaml`, asserts `diff` and `push` return `fix_validation` without a plan, journal, or push id, and fetches Notion to prove the row was not mutated. Covers E2E-017. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_remote_fast_forward_updates_clean_file_and_preserves_pending_file` | Live virtual filesystem core | Hydrate a clean virtual file, mutate remote, fast-forward local content; then add local pending edit and assert remote fast-forward skips without overwrite. Covers E2E-025. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_scheduled_pull_queues_and_applies_remote_fast_forward` | Live mounted workflow, scheduled pull | Run scheduled pull enumeration against a scratch parent/child tree, hydrate the child, mutate it remotely, run scheduled pull again, verify a `RemoteFastForward` hydration request is queued, drain it, and verify local Markdown updates. Covers live scheduler trigger behavior for E2E-024 and E2E-025. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_scheduled_pull_idle_ticks_do_not_repeat_notion_enumeration_or_queue_duplicates` | Live mounted workflow, scheduled pull | Wraps the real Notion scheduled-pull source with counters, runs an initial poll, repeated sub-interval idle ticks, and one due tick, then verifies idle ticks do not enumerate Notion or grow queued hydration work while due ticks still poll at the configured interval. Covers the live idle-budget side of E2E-024. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::scheduled_pull_large_workspace_stubs_metadata_and_queues_only_root_hydration` | Local scheduled workflow | Runs scheduled pull against a deterministic mounted tree with 32 child pages, verifies metadata stubs are written without child body hydration, and asserts only the root policy hydration is queued. Covers the local bounded-work side of E2E-024. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::scheduled_pull_idle_ticks_do_not_enumerate_or_queue_duplicate_hydrations` | Local scheduled workflow | Runs an initial scheduled poll, leaves the root hydration pending, advances repeated sub-interval idle ticks, and verifies the remote tree is not enumerated again and queued hydration work stays bounded. Covers the local idle-budget side of E2E-024. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::workspace_virtual_freshness_active_and_cold_ticks_prioritize_hot_work_and_cap_scan_budget` | Local workspace virtual freshness workflow | Seeds a workspace-level virtual mount with dirty, conflicted, recently opened, remote-hint, stub, virtual, and 130 warm hydrated pages, verifies active ticks queue only hot user-relevant work, verifies cold ticks cap background observation jobs at 100, and asserts stub/virtual pages do not consume freshness budget. Covers the workspace-virtual active/pending and bounded-budget side of E2E-024 without remote enumeration. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_inspect_explains_remote_and_local_drift_without_mutating` | Live mounted workflow, plain files | Pull scratch page, inspect all-synced state, mutate remote and inspect safe fast-forward, add local drift and inspect review-needed state, while verifying local file content is unchanged by inspect. Covers E2E-026. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_push_log_and_undo_restores_remote_content` | Live mounted workflow, plain files | Push a scratch page edit, verify journal/log preimage and apply-effect metadata, run connector-backed undo, verify Notion remote content is restored and journal status becomes reverted. Covers E2E-027. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_directive_block_move_undo_restores_remote_order` | Live mounted workflow, plain files | Move a `table_of_contents` directive through Notion's copy+archive move path, undo the reconciled journal, and verify the remote Markdown order is restored with a single directive block. Covers copy+archive move undo for E2E-027. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_undo_archive_restores_paragraph_link_without_link_to_page_promotion` | Live mounted workflow, plain files | Delete a paragraph block whose visible Markdown is `[Linked page](...)`, push the archive, undo it, and verify Notion restores a paragraph block instead of promoting it to `link_to_page`. Covers archived-block undo fidelity for E2E-027. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_undo_child_page_create_archives_remote_page` | Live mounted workflow, plain files | Create a child page from a local `page.md`, verify the create journal effect, undo the push with the Notion undo applier, and verify Notion archives the created child page. Covers created-entity undo for E2E-027. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_undo_database_row_create_archives_remote_row` | Live mounted workflow, plain files | Create a database row from a local row-directory `page.md`, verify the create journal effect and undo plan, undo the push with the Notion undo applier, and verify Notion archives the created row page. Covers database-row created-entity undo for E2E-027. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_page_directory_create_pushes_child_page_and_refreshes_parent` | Live mounted workflow, plain files | Create a new local child page directory with `page.md`, push it to Notion, verify the child page body, and verify the parent local/remote Markdown refreshes with the child-page link. Covers the create side of E2E-020. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_locate_notion_url_returns_markdown_path_and_can_prioritize_hydration` | Live virtual filesystem core | Index scratch page metadata, locate by Notion URL, assert Markdown file path and online-only state, then hydrate located page. Covers E2E-007. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_cyclic_diverse_page_read_noop_preserves_notion` | Live mounted workflow, plain files | Create page with diverse blocks and references, render expected Markdown, no-op push, verify Notion block JSON unchanged. Covers E2E-008, E2E-009, E2E-015 read paths. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_code_block_with_embedded_fence_edits_round_trip` | Live mounted workflow, plain files | Edit a Notion code block whose content includes an embedded triple-backtick fence, verify Locality chooses a longer outer fence, push, and reconcile cleanly. Covers code-fence round-trip fidelity for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_code_block_ignores_fence_marker_with_trailing_text` | Live mounted workflow, plain files | Edit a code block containing a line that looks like a fence opener but has trailing text, verify it remains inside the code block, and reconcile cleanly. Covers code-fence parser edge cases for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_text_code_fence_alias_pushes_as_plain_text` | Live mounted workflow, plain files | Edit a Notion `plain text` code block using a Markdown `text` fence and verify push normalizes back to Notion's `plain text` language. Covers code-language alias handling for E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_cyclic_supported_block_edits_push_and_verify_notion` | Live mounted workflow, plain files | Edit supported rich blocks and typed mentions through Markdown, push, fetch/verify Notion render. Covers E2E-014, E2E-015. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_local_image_media_edit_uploads_and_reconciles_bytes` | Live mounted workflow, plain files | Overwrite a downloaded local image file, edit its Markdown caption, push the upload to Notion, and verify the reconciled local image bytes remain local. Covers image upload behavior for E2E-010 and E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_local_image_media_edit_with_escaped_caption_uploads_and_reconciles` | Live mounted workflow, plain files | Overwrite a downloaded local image file, edit its caption to include escaped Markdown link-label characters, push the upload to Notion, and verify the reconciled local image bytes remain local. Covers escaped media-caption planning for E2E-010 and E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_local_file_like_media_appends_upload_and_reconcile_local_links` | Live mounted workflow, plain files | Append local video, PDF, audio, and HTML links from `.loc/media`, push uploads to Notion, and verify reconciled Markdown uses local media links with non-empty materialized files. Covers file-like media upload behavior for E2E-010 and E2E-014. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_cyclic_database_rows_mount_edit_create_and_verify_notion` | Live mounted workflow, plain files | Create database, pull schema, hydrate row, no-op preserves row JSON, edit row properties/body, create row from new Markdown file, fetch/verify Notion. Covers E2E-016, E2E-018, E2E-019. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::live_database_row_directory_create_pushes_row_and_reconciles` | Live mounted workflow, plain files | Create a new database row as `database/new-row/page.md`, verify the create plan, push to Notion, and verify the reconciled row file is clean with the expected remote properties/body. Covers the page-directory row creation variant for E2E-019. |
| `crates/loc-cli/tests/e2e_push_workflow.rs::multi_data_source_database_row_create_blocks_before_journaled_apply` | Local mounted workflow | Creates a draft database row under a projected database directory whose `_schema.yaml` contains multiple Notion data sources, then verifies both `diff` and `push` return `notion_schema_ambiguous_data_source` without a plan, journal entry, push id, file reconciliation, or connector apply call. Covers the unsupported multi-data-source row-create guardrail for E2E-019. |
| `apps/desktop/src-tauri/src/main.rs::tests::live_mode_bidirectional_cloudstorage_markdown_e2e` | Live macOS CloudStorage desktop workflow | Runs the desktop Live Mode tick loop against an explicit scratch CloudStorage `page.md`, performs three local-to-Notion edits and three Notion-to-local edits, verifies each direction through the file and API, then removes test marker blocks. Covers Live Mode bidirectional sync on a real File Provider-visible page. |
| `tests/live_notion_vfs_push_pull.sh` | Live Linux FUSE provider | Reads `NOTION_TOKEN` or the installed `~/.loc/credentials` Notion connection, creates a scratch Notion page when `LOCALITY_NOTION_LIVE_PARENT_PAGE` is set, mounts it through the real `locality-fuse` kernel mount with token env removed from `loc`/`localityd`, hydrates `page.md`, edits and pushes it, creates a child page directory through FUSE and verifies the Notion child, renames the child directory and verifies the remote title, deletes the child directory and verifies the remote page is archived. Covers Linux provider paths for E2E-004, E2E-005, E2E-006, E2E-011, E2E-013, and E2E-020. |
| `tests/windows_cloud_files_live.ps1` | Live Windows Cloud Files provider | Registers a real Cloud Files sync root, lazily enumerates a scratch Notion page, hydrates `page.md`, edits and pushes it, creates/renames/deletes a pending draft, creates a child page directory through the mount, pushes it to Notion, then deletes and pushes the child archive. Covers Windows provider paths for E2E-004, E2E-006, E2E-011, E2E-013, and E2E-020. |
| `crates/loc-cli/tests/projection_contract.rs` | Local shared projection contract | Runs the same daemon virtual-filesystem browse, hydrate, write, create, rename, and delete contract against macOS File Provider, Linux FUSE, and Windows Cloud Files projection modes below the OS adapters. Covers the shared semantics behind E2E-004, E2E-005, E2E-006, E2E-011, and E2E-020. |
| `crates/loc-cli/tests/doctor.rs` | Local diagnostics contract | Verifies `loc doctor` does not initialize missing state and reports mount, connection, profile, and credential findings with recovery commands. |
| `crates/loc-cli/tests/live_workspace_mount.rs::live_workspace_pull_edit_pull_push_regression` | Manual/destructive live workspace | Pull workspace/file, edit mounted file, dirty pull skip, push, pull, verify content. Covers parts of E2E-011, E2E-013, E2E-022, but is ignored and uses a pre-existing workspace. |
| `crates/locality-notion/tests/fetch_render.rs::live_fetch_and_render_page_from_environment` | Manual live fetch/render | Fetch/render one configured page. Legacy smoke; lower coverage than the scratch live tests. |

## Current Live Coverage Summary

| Area | Live status | Notes |
|---|---|---|
| Notion page read/write integrity | Strong | Scratch pages are created, edited, pushed, fetched, and verified. |
| Rich block coverage | Strong for supported block types | Representative supported blocks are read and edited live. Unsupported/layout blocks are read/no-op protected. Literal Markdown/HTML marker, parenthesized link, and code-fence parser edge cases are covered by live mounted workflow tests. |
| Database schema and row writes | Strong | Schema, validation, property update, body update, and row create are covered. |
| Media download/upload | Covered for common file-like media | Live tests download image, video, file, PDF, and audio assets locally; upload edited image bytes; and append local video, PDF, audio, and HTML uploads while verifying local links after reconciliation. Local e2e verifies `.loc/media` manifest writing and missing-file repair; pruning/eviction remains not covered. |
| Mounted workflow | Good for core, partial for platform kernels | Live tests cover plain-file mounted workflow through library and real `loc` binary paths, virtual filesystem lazy paths, Linux FUSE against live Notion, Windows Cloud Files registration, macOS CloudStorage Live Mode against live Notion, and a macOS File Provider conflict-marker cache/visible-replica path. Broader macOS File Provider create/rename/delete coverage remains open. |
| Shared root mount points | Covered by Linux FUSE smoke, macOS File Provider domain children tests, Windows Cloud Files live test | Verifies one Locality root exposes multiple mount-point folders mapped to distinct mount IDs/connectors. |
| Desktop onboarding/tray/review UI | Partial live | Live Mode sync semantics are covered through the desktop tick loop against a real CloudStorage page, and local e2e verifies daemon auto-save applies safe edits while blocking destructive plans before journal/apply; onboarding, tray, and review UI automation remain manual. |
| CLI diagnostics and local recovery | Covered live for plain-file mounted workflow and local virtual projection workflow | The stored-credential live binary workflow covers `info`, `doctor`, and `restore` against a scratch Notion-backed mount with token env removed. Local e2e coverage verifies virtual projection restore rewrites the daemon content cache and returns status clean. |
| Read-only mount policy | Covered live for plain-file mounted workflow | The stored-credential live binary workflow verifies a read-only scratch mount blocks local push planning before journal/remote writes and leaves Notion unchanged. |
| Freshness/drift/auto-fast-forward | Partial live | Live tests cover drift preflight, dirty-pull conflict recovery, fast-forward apply/skip behavior, scheduled pull queuing/applying a remote fast-forward, and scheduled-pull idle ticks against real Notion without repeated enumeration or duplicate queue growth. Local e2e coverage verifies larger-tree scheduled pulls stay as metadata stubs, queue only policy hydration, keep idle ticks from enumerating or growing queued work, and cap workspace-virtual freshness jobs while prioritizing dirty/conflicted/active/remote-hint pages; long-running wall-clock/API budget assertions remain a gap. |
| OAuth broker | Local only | Secret separation is tested in connector-focused tests and in the workflow e2e file with the file credential store and connection report surfaces; real OAuth UX/broker round-trip is manual. |
| Google Docs connector | Local workflow guardrails | Local e2e covers Google Docs mounted Markdown guardrails for rendered inline objects, tables, invalid Locality frontmatter, and unsupported document-structure directives before journal/apply. Live Google Docs read/write and OAuth broker smoke remain open. |
| Packaging/notarization | Manual/publish covered | `make publish` validates signing, stapling, and DMG integrity outside CI. |

## Remaining Recommended Live E2E Additions

Implemented in `crates/loc-cli/tests/e2e_push_workflow.rs`: live lazy virtual
mount, virtual page rename/delete, drift preflight, remote fast-forward, and URL
locate coverage.

1. **macOS kernel live mount e2e**: run live Notion lazy enumeration and
   create/rename/delete through signed macOS File Provider locally or on a
   dedicated Mac. Linux FUSE is covered by the live Notion workflow on runners
   where `/dev/fuse` and live Notion credentials are available.
2. **Daemon scheduler budget e2e**: extend the live scheduler coverage beyond
   per-tick Notion enumeration counters into a long-lived daemon run that
   asserts bounded API work over wall-clock time.
3. **Desktop app smoke e2e**: run the Tauri app UI against a disposable state
   dir and fake/live backend, verify onboarding reset, pending changes refresh,
   non-blocking push, success confirmation, tray state, and low idle CPU.
4. **OAuth broker smoke**: use a test Notion integration and broker deployment
   to verify the local client receives/stores only the broker refresh handle.
5. **Google Docs live e2e**: use a test Google OAuth integration and scratch
   Drive folder to verify connect, mount, pull, guarded edits, push, and cleanup
   against the real Google Docs and Drive APIs.
